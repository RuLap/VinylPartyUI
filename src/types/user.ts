export interface UserGet {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}

export interface UserSet {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}